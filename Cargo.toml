[package]
name = "wolfplayer"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = "0.18"
glib = "0.18"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

tokio = { version = "1", features = ["rt-multi-thread", "process"] }
